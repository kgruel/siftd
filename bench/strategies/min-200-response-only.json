{
  "name": "min-200-response-only",
  "description": "200-char minimum, only index response blocks",
  "goal": "Aggressive pruning — remove short chunks and all prompts to test if responses alone carry enough signal",
  "params": {
    "min_chars": 200,
    "chunk_types": ["response"],
    "concat": false
  }
}
